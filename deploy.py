#!/usr/bin/env python3
"""LEGACY / DISABLED (2026-08-02).

This former deploy and indexing automation is intentionally non-executable.
Publishing is allowed only through the current Blog-Publish-Gate workflow
with an explicit, separate user approval. No Git, deploy, promotion, or
indexing action is performed here.
"""
import sys

print("Dieses Legacy-Deploy-Skript ist deaktiviert. Veröffentlichungen erfolgen ausschließlich über den aktuellen Blog-Publish-Gate-Workflow.")
sys.exit(64)
