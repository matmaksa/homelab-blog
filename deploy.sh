#!/usr/bin/env bash
# LEGACY / DISABLED (2026-08-02).
# This former deploy/push/indexing script must never publish automatically.
printf '%s\n' 'Dieses Legacy-Deploy-Skript ist deaktiviert. Veröffentlichungen erfolgen ausschließlich über den aktuellen Blog-Publish-Gate-Workflow.' >&2
exit 64
